<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.irad9731.loopdeloopover">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Board5X5Activity" />
        <activity android:name=".Board7X7Activity" />
        <activity android:name=".Board9X9Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GameFinishedActivity" />
        <activity android:name=".LeaderBoard" />

        <service
            android:name=".NewSheriffInTownService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>